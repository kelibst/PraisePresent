# P3-D4 — Media domain
- **ID:** 2026-06-26_p3-d4-media
- **Phase:** 3
- **Assigned agent type:** implementer + reviewer + tester
- **Status:** pending

## Goal
MediaService + mediaRepository; library with folders/tags; image/video/audio + backgrounds (lean on Chromium/FFmpeg); handle corrupt files gracefully.

## Scope
- `src/main/db/`: media migration; `mediaRepository`. `src/main/services/mediaService.ts`. `src/renderer/features/media/`. Video background on the audience window.

## Rules
- §5.5, §5.7 (corrupt files don't crash).

## Acceptance
- [ ] import/organize media; video background plays smoothly on audience window
- [ ] corrupt files handled gracefully (no crash)
- [ ] unit + e2e; reviewer sign-off; **observed running**

## VERIFICATION LIMIT
- Video playback smoothness needs observed verification + sample media files.

## Outcome (2026-06-27 — DONE, reviewer PASS + security SIGN-OFF)
Built the full Media vertical slice. User chose **reference original paths** (no copy); files fail safe to black if moved/deleted.
- **Backend:** migration 6 `media` table (path UNIQUE, idempotent); `mediaRepository` (list/getPath/add/remove, parameterized); `mediaService` (list/add/import/remove — `import` opens the OS picker in main; `add` is the single registration path, skips unsupported AND non-existent files); pure `mediaClassify` (ext→kind) + unit tests; `media:list/import/add/remove` zod-validated channels.
- **Secure file serving:** new `app-media://` custom protocol (`mediaProtocol.ts`) registered privileged (standard/secure/supportFetchAPI/stream, NOT bypassCSP) before app-ready; handler resolves `app-media://media/<id>` → path via the DB **allow-list** (a path never comes from the URL → no traversal), 400/404 on bad/unknown id, streams via `net.fetch`. CSP (`csp.ts` + `index.html` meta) extended with `img-src ... app-media:` and `media-src 'self' app-media:` only. `mediaUrl` single-sourced in `src/shared/constants/media.ts`.
- **Present integration (extended, NOT forked):** `presentSlide` gained optional `media: { kind, url }` (§1.9). `AudienceView` renders image/video/audio full-screen with text overlay, fails safe to black on media `onError`. Added `autoplayPolicy: 'no-user-gesture-required'` so projector video/audio plays without a click.
- **UI:** `MediaPage` — Add media (dialog), library grid with image thumbnails + kind icons, click-to-present, remove (does NOT delete the original), Black, empty state.
- **Tests:** `tests/e2e/media.spec.ts` — add by path, skip unsupported, library renders, present → audience `<img>` served over `app-media://` and actually decodes (naturalWidth>0), unknown-id 404 fail-safe, remove keeps the original file. All green: tsc 0 · lint 0 · **65 unit · 13 e2e**.
- **Reviews:** reviewer PASS, security SIGN-OFF (allow-list-by-id holds against traversal; CSP scoped to img/media; hardening intact). Adopted hardening: `mediaRepository.add` reads id back by path (correct on re-add); `mediaService.add` now skips non-existent paths (defense-in-depth on the allow-list); MediaPage Black button focus-visible.

## VERIFICATION LIMIT (carried — needs the user)
Image display is fully auto-verified (e2e decodes a real PNG via the protocol). **Video/audio playback smoothness on the audience window is NOT machine-verifiable here** (headless, single display): wiring + fail-safe are tested, but the user should drop a sample video/image and observe playback + fps on a real second screen (same class as the D2 dual-monitor observed run).
