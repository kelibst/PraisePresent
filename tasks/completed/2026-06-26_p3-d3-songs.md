# P3-D3 — Songs domain
- **ID:** 2026-06-26_p3-d3-songs
- **Phase:** 3
- **Assigned agent type:** implementer + reviewer + tester
- **Status:** done

## Goal
SongService + songRepository; schema (title/author/CCLI/tags + verse/chorus/bridge sections); editor; import (plain text / OpenLyrics / CCLI); arrangement ordering; projection formatting; present to audience.

## Scope
- `src/main/db/`: songs migration; `songRepository`. `src/main/services/songService.ts`. songs IPC channels/schemas + bridge. `src/renderer/features/songs/` (list/create/edit/present).

## Rules
- §5.5, §5.9, R17 (CCLI reporting hook).

## Acceptance
- [ ] create/import/edit/present a song; CCLI metadata captured
- [ ] sections (verse/chorus/bridge) + arrangement order
- [ ] present a song slide → audience mirrors
- [ ] unit + e2e; reviewer sign-off

## Outcome
**2026-06-26.** Complete vertical slice on the Phase 2 foundation. Migration 3 (songs + song_sections, FK ON DELETE CASCADE); `songRepository` (parameterized, atomic transactions); `songService` + pure `parsePlainText` importer (verse/chorus/bridge, [Label] headers, CRLF-normalized); songs IPC (list/get/create/update/delete/importText, zod-validated) + typed `window.api.songs` bridge; `SongsPage` (library, plain-text import, present sections to audience) + `/songs` route. **21 unit + 6 e2e** — e2e imports a song through the bridge→SQLite, lists it, projects a section to the audience window, and renders the SongsPage UI. Reviewer **PASS**; applied review fix (CRLF parsing).
- **Done out of brief order:** D3 built before D1/D2 because Scripture (D1) needs a licensed Bible dataset/API not available here; Songs is self-contained + fully verifiable, proving the domain pattern.
- **Deferred (non-blocking):** edit/delete UI affordances; CCLI reporting hook (R17); OpenLyrics import; prechorus/tag/intro/outro never produced by plain-text parser.
