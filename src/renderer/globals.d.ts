// Build-time constants injected by Vite `define` (see vite.renderer.config.ts).
// `__APP_VERSION__` carries the package.json version into the renderer for the
// StatusStrip — a public string, no IPC needed (CLAUDE.md §1.3).
declare const __APP_VERSION__: string;
